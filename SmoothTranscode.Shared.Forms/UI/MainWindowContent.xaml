<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SmoothTranscode"
    xmlns:ui="clr-namespace:SmoothTranscode.UI"
    x:Class="SmoothTranscode.UI.MainWindowContent"
    Background="{ui:AppThemeBinding Light=White, Dark=Black}"
    Title="SmoothTranscode">
    <ContentPage.Content>
        <ListView Background="Transparent" GroupDisplayBinding="{Binding Source.FileName}" ItemsSource="{x:Static local:EncodeManager.JobQueue}" IsGroupingEnabled="True" SelectionMode="None">
            <ListView.GroupHeaderTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame Background="{AppThemeBinding Light=#EEEEEE, Dark=#212121}" CornerRadius="5" Margin="15, 0, 15, 20" Padding="15, 0" HasShadow="False">
                            <Label FontSize="Small" Text="{Binding Source.FileName}" VerticalOptions="Center" />
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.GroupHeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>
