<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SmoothTranscode"
             x:Class="SmoothTranscode.Maui.UI.MainPage"
             Title="SmoothTranscode"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackLayout BackgroundColor="#E5E7EB" Orientation="Horizontal">
            <Button Padding="20, 0" Text="Add File" Clicked="AddFileClicked" />
            <Button Padding="20, 0" Text="Start Conversion" Clicked="StartConversionClicked" />
        </StackLayout>

        <CollectionView Grid.Row="1" ItemsSource="{Binding JobQueue}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Source.FileName}" BackgroundColor="#ECEFF1" Padding="20" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
